<?php
/**
 * 医学影像浏览器 — 可选配置
 * 本文件默认所有配置均被注释(即:不启用登录、数据存放在程序目录 data/)。
 * 需要启用某项时,去掉对应 define 前的注释并修改取值。
 */

// —— 访问认证(建议开启,尤其是 NAS 暴露在公网时)——
// 设置后,打开页面需先登录。两个值都留空('')则不启用登录。
// define('MV_USER', 'admin');
// define('MV_PASS', '你的密码');

// —— 数据目录(默认为本程序目录下的 data/)——
// 如需把影像存储到别的大容量共享文件夹,可取消注释并修改:
// define('MV_DATA_DIR', '/volume1/medicalviewer-data');
