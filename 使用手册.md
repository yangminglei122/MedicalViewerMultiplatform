# 医学影像浏览器 · 使用手册

一个装在群晖 NAS 上的看片工具。电脑和手机浏览器打开同一个网址就能用,不需要装任何软件。

---

## 1. 登录

浏览器打开 `http://192.168.8.217:8160`(换成你 NAS 的地址和端口),输入账号密码。

- 首次部署默认账号 **admin / admin**,登录后请立刻修改密码(右上角 → 账号管理 → 改密)。
- 管理员可以开临时账号给别人用,可以设定有效期、可以限制"不许下载图像"。

---

## 2. 首页:患者列表

打开后就是患者列表,按患者分组,和 PACS 工作列表类似。

- **点患者那一行** → 展开显示该患者每次检查;再点一次收起。
- **点检查那一行** → 直接进入看片。
- **检查行最左边的小箭头** → 展开该检查所有序列的缩略图,先扫一眼再决定开哪个。
- 顶部**搜索框**:姓名、ID、检查描述都能搜。
- 搜索框下面可以按**日期范围**和**设备类型**(CT / MR / DX…)筛选,可多选。
- 每行右边的图标:**铅笔**改患者信息、**下载**导出、**垃圾桶**删除(有二次确认)。

---

## 3. 导入影像

点右上角**导入**,选 DICOM 文件、整个文件夹或 ZIP 包;也可以直接把文件**拖进窗口**。

上传解析完会弹出**"确认患者信息"对话框——这是唯一决定数据归属的地方,请认真核对**:

- 姓名、ID、出生日期、性别都可以直接改(影像文件里的信息经常是错的,以你改的为准)。
- 如果库里已有**同名同生日**的患者,会提示"合并到该患者"。
- 如果只有**同名**但生日不同,会列出库里同名患者,点一下即可一键填入库内信息;也可以自己手填。
- 每组检查前有**「导入 / 跳过」**切换:亮着"导入"的才会入库;想不导就切成"跳过"。
- 右下角按钮显示**「确认导入 (n/总数)」**,点它完成入库。

> 什么是"一组"?同一个患者的一批文件算一组。多人混在一起导也没关系,会自动按患者分组逐个确认。

**删除数据**:首页对应行点垃圾桶 → 确认。原始文件会一起删除,不可恢复,删前想清楚。

---

## 4. 看片:鼠标操作速查

| 想做什么 | 操作 |
|---|---|
| 调窗宽窗位(亮度对比度) | **按住左键拖动**(左右=窗宽,上下=窗位) |
| 缩放 | **按住右键上下拖** 或 Ctrl+滚轮 |
| 平移图像 | **按住中键拖** 或 Shift+左键拖 |
| 翻层(上一张/下一张) | **滚轮** |
| 复位(回到初始状态) | **双击图像** |
| 看某处 CT 值 | 鼠标悬停,左下角显示坐标和 HU |
| 微调窗位 | ← → 方向键(加 Shift 一次调 50) |
| 翻层 | ↑ ↓(PageUp/PageDown 一次 10 层) |
| 旋转 R / 反色 I / 复位 F / 播放 空格 | 键盘快捷键 |

**手机触屏**:单指拖=当前工具,双指捏合=缩放,双指拖=平移;右侧竖条是翻层滑块。

## 5. 工具栏按钮(从左到右)

- **返回 / 序列**:回列表;打开序列抽屉切换序列(有序列缩略图)。
- **窗位预设下拉**:按设备给常用窗——CT 有肺窗/纵隔/骨窗/脑窗/软组织,MR 有脑 T1/T2 等,DR 有自动全范围。
- **ROI窗**:按一下激活,在图上**拉一个框**,程序按框内组织自动算出最合适的窗宽窗位(X 光片特别好用)。
- **MPR**:三维重建,见第 7 节。
- **平滑**:放大时图像平滑(开)还是显示原始像素(关)。
- **十字线**:仅 MPR 模式,显示/隐藏参考线。
- **调窗 / 平移 / 缩放 / 滚动**:切换左键的功能。
- **长度 / 角度 / 矩形 / 椭圆 / 文字**:测量标注,见第 6 节。
- **反色 / 左旋 / 右旋 / 水平翻转 / 垂直翻转 / 复位**:图像变换。
- **单图 / 双图 / 三图 / 四图**:分屏布局。双图/三图/四图时**先点一下某个格子**(出现蓝框=激活),再点序列就装进那个格子,可同屏对比多个序列。
- **导出PNG**:把当前画面(含标注)存成图片。
- **导出DICOM**:整个检查打包成 ZIP 下载(账号没这个权限时按钮不显示)。
- **清除标注**:清空当前序列所有标注。

底部:**层号滑块**(拖动快速翻层)、**▶ 播放**(CINE 动态播放,可调帧率)。

---

## 6. 测量标注

选中工具后在图上**按住左键拖**画出标注(角度工具点三下)。

- **长度**:自动按毫米显示(前提是影像带像素间距,否则显示像素)。
- **矩形 / 椭圆**:框选后自动算**面积、均值 ± 标准差**——量 CT 值就是它。
- 标注**跟着层走**:每层的标注独立存放,翻层不会乱。
- **修改**:点标注选中(端点变白),拖端点调整位置;出现红叉圆点,点它删除。
- **删除**:按 Delete 键删选中的,或工具栏"清除标注"清空整个序列。
- 标注只存在内存里,**刷新页面会消失**,截图或导出 PNG 保存结果。

---

## 7. MPR(三维切面)

需要**同一序列 8 层以上**的 CT/MR(比如 40 层的 T2)。点工具栏 **MPR**,等进度条走完,出现三个切面:轴位(横断面)、冠状位、矢状面。

- **十字线**:点哪个切面,其他两个切面跟着切到那个位置——三平面联动。
- 底部滑块 = 层位置;每格可单独缩放平移。
- 窗宽窗位预设对 MPR 同样有效。
- **再点一次 MPR** 退出,回到普通浏览。
- DR、不足 8 层的序列没有 MPR。

---

## 8. DWI 等交织序列:自动拆分同屏

同一序列里混着的几组图(比如 DWI 的 b0 / b50 / b1000,每层几张交织在一起),系统会**自动拆成几个子序列**(名字带 [1/3]、[2/3]、[3/3]),点开后**按组员数量自动分屏**——两组双屏、三组三屏(手机上自动竖排)、四组四屏,**翻层和调窗全部同步**。

各组图像灰度范围不一样,调窗时各屏会**按各自的灰度自动换算**,亮度始终匹配,不会出现一边调好了另一边全黑。

---

## 9. 账号管理(仅管理员)

右上角 → **账号管理**:

- **创建临时账号**:设密码、有效期(1 天~1 年)、备注(给谁用);**"允许下载图像"**不勾,该账号看不到任何导出按钮、后端也会拒绝其下载请求。
- 临时账号到期自动失效,不用手动停。
- 每行可 **+7天** 续期、**改密**、**删除**。

---

## 10. 常见问题

**页面显示不对 / 改了没生效**
浏览器缓存了旧文件。强制刷新:**Ctrl + F5**(手机:关掉标签页重开)。

**手机打不开**
确认手机和 NAS 在同一网络,地址带 `http://` 和正确端口。

**某几张图报"不支持的压缩格式"**
该影像用了 JPEG2000 压缩,目前不支持。需要在工作站上把它转存为未压缩或普通 JPEG 后再导入。

**导入时提示"批量上传受限"**
PHP 上传上限偏小,程序会自动改逐个上传,能导完,只是慢。按 README 调大 `post_max_size` 后重试。

**年龄显示的是什么年龄?**
按检查日期算的拍摄时年龄(不是现在的年龄),每次检查各自显示。

---

*版本 v1.7.3 · 详细部署见 README.md*
